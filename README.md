# Basic twitter clone
Inlcudes server and client. 


## Setup

### Server
Express.js for api;
Ws package for websocket server
- `cd server`
- `npm i && npm run dev`

### Client
Uses vite for build; 
Tailwind for styles; 
Shadcn for core components
- `cd client`
- `npm i && npm run dev`
